{
  "name": "flora-hive",
  "version": "0.1.0",
  "description": "Flora Hive — MQTT bridge and authenticated HTTP API",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "docker:build": "docker compose build",
    "docker:up": "docker compose up -d"
  },
  "engines": {
    "node": "^20.0.0"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "mqtt": "^5.10.4"
  }
}
