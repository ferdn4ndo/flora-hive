## Summary

<!-- What does this PR change and why? -->

## Checklist

- [ ] `make build` and `make test` pass locally (use `make test-race` before merge if you changed concurrency-sensitive code)
- [ ] `.env.example` updated if new configuration is required
- [ ] README or OpenAPI notes updated if API surface changed
